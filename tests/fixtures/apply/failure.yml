---
- name: This one works
  shell: "true"
  changed_when: "false"

- name: This one does not
  shell: |
    echo "context line" >&2
    echo "sed: can't read /etc/pacman.conf: No such file or directory" >&2
    exit 2
  changed_when: "false"

- name: Never reached
  shell: "true"
  changed_when: "false"
